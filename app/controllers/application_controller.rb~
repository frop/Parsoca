class ApplicationController < ActionController::Base
  protect_from_forgery
  
  before_filter :extra_info

  def index
    redirect_to "/intervals" if user_signed_in?
  end
  
  def extra_info
    redirect_to "/extra_info" if user_signed_in and current_user.time_bank.nil?
  end
end
